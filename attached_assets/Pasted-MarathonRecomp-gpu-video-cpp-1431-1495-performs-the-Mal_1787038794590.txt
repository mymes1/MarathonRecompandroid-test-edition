MarathonRecomp/gpu/video.cpp:1431-1495 performs the Mali vertex upload from the guest snapshot, copies the entire dataSize, converts only declaration fields beginning at streamOffset, and caches by frame serial, snapshot generation, declaration pointer, stride, and stream offset. MarathonRecomp/gpu/video.cpp:1498-1524 then adds offset to the cached allocation reference. This is internally consistent: the upload allocation remains buffer-relative, while the render view begins at the D3D stream offset. ProcSetStreamSource also sets the view size to dataSize - offset at lines 7541-7548. SwapMaliVertexElements correctly avoids the prior whole-buffer 32-bit swap corruption. At 1371-1379 it treats the stream as byte-structured data and starts at streamOffset; at 1385-1425 it swaps per declaration field, preserving UBYTE fields and handling 16-bit components separately. Guest snapshot lifetime is correctly handled for Mali: UnlockBuffer snapshots before returning at 3979-4011; vertex/index draw paths select that snapshot at 1469-1470 and 1279-1281; cache generation is compared at 1451-1456 and 1271-1274. Draw binding is refreshed at the actual draw boundary (6852-6857, 6894-6900) and dirty vertex/index views are updated by RefreshMaliActiveBufferViews (1544-1597). Concrete bug found: failed vertex uploads do not invalidate the vertex-frame cache. In UploadMaliGuestVertexBuffer, the no-source path at 1469-1477 clears buffer->maliFrameReference but does not clear cache (buffer->maliVertexFrameCache[stream]). More importantly, the malformed empty/unstrided path clears the cache at 1444-1449, but the no-source path does not. If the same buffer/stream retains the same frame serial, snapshot generation, declaration, stride, and offset, the cache can still satisfy the fast-path test at 1451-1458 and return a stale upload reference despite the source being unavailable. This can bind stale vertex data rather than fail safely. Minimal fix: in the no-source branch at lines 1472-1477, add: cache = {}; Also clear cache in the analogous index no-source/malformed paths if the index cache is later split out; current index state uses maliFrameReference directly and is properly invalidated there. No stream-offset, endian, snapshot-generation, or draw-binding corruption was otherwise evident in the audited path. thirdparty/plume/plume_render_interface_types.h:971-985 makes VertexBuffer set RenderBufferFlag::VERTEX and IndexBuffer set INDEX; thirdparty/plume/plume_vulkan.cpp:932-934 maps those to VK_BUFFER_USAGE_VERTEX_BUFFER_BIT/INDEX_BUFFER_BIT. Thus CreateVertexBuffer/CreateIndexBuffer (MarathonRecomp/gpu/video.cpp:5203-5228) are valid. The comments at video.cpp:5206-5209 document the former failure mode, but it is not present now. Staged DEFAULT buffers advertise TRANSFER_DST (plume_vulkan.cpp:943-950, and again 977-980); UPLOAD buffers advertise TRANSFER_SRC (981-984). This matches the copy direction. Host writes are explicitly flushed on unmap/range (plume_vulkan.cpp:1042-1063, 1066-1075). Mali guest vertex/index unlock snapshots CPU data (video.cpp:3993-4011), then per-frame uploads use g_uploadAllocators references (video.cpp:1255-1299, 1431-1495), avoiding reuse races. Mali disables gpuUploadHeap (video.cpp:3178-3185). Non-Mali staged copies issue COPY->GRAPHICS barriers around vkCmdCopyBuffer (video.cpp:4044-4049). Vulkan barriers track the prior producer stage (plume_vulkan.cpp:3192-3217) and submit a real vkCmdPipelineBarrier with TRANSFER->vertex-input/access masks (680-710, 713-740, 3290-3295); this fixes the classic TOP_OF_PIPE/stale Mali fetch issue. Texture uploads transition to COPY_DEST, flush a COPY-only barrier, copy, then transition to SHADER_READ (video.cpp:3893-3911; rect unlock 3932-3957). The Vulkan image barrier preserves old layout and tracked producer stage (plume_vulkan.cpp:3260-3283). Mali descriptor writes are shadowed and replayed per frame only before that slot begins recording (video.cpp:448-550, 2822-2854), avoiding mutation of in-flight bindless sets; Mali uses fixed non-update-after-bind layout (553-568). Proposed minimal fix if diagnosing an older revision: (1) ensure RenderBufferDesc::VertexBuffer/IndexBuffer set VERTEX/INDEX and Vulkan maps them; (2) add TRANSFER_DST to DEFAULT destinations; (3) flush mapped writes; (4) record COPY->GRAPHICS barriers on the same command buffer/queue before draw; (5) defer descriptor writes per frame. Those changes are already represented at the cited lines. If corruption persists on SM-X110, next target is outside these requested files (shader/input layout or Mali upload allocator data conversion), not a currently evident command/barrier/descriptor contract violation.